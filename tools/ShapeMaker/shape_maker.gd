@tool #This annotation indicates the script runs in the editor.
extends Node2D

# -- Enums --
# Define the shape types as an enum for user-friendly selection in the Inspector.
enum ShapeType {
	CIRCLE,
	RECTANGLE
}
